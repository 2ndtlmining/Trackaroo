# Trackaroo environment variables
# Copy to .env (gitignored) and fill in overrides. All values optional —
# the known-good read-only defaults are baked into scraper/pccg.py.

# Algolia credentials embedded in PCCG's page source.
# These are READ-ONLY public search keys (safe to distribute; cannot write or delete).
ALGOLIA_APP_ID=HPD3DBJ2IO
ALGOLIA_API_KEY=9559cf1a6c7521a30ba0832ec6c38499